name: CI/CD Main Pipeline

permissions:
  contents: write
  id-token: write
  issues: write
  pages: write
  pull-requests: write

on:
  push:
    branches:
      - main
      - develop

jobs:
  lint:
    uses: kurocado-studio/devops/.github/workflows/workflow.lint.yml@main
    secrets:
      NPM_TOKEN: ${{ secrets.NPM_TOKEN }}
      GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    with:
      monorepo: true

  test:
    needs: lint
    uses: kurocado-studio/devops/.github/workflows/workflow.test.yml@main
    secrets:
      NPM_TOKEN: ${{ secrets.NPM_TOKEN }}
      GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    with:
      monorepo: true

  document:
    needs: test
    uses: kurocado-studio/devops/.github/workflows/workflow.document.yml@main
    secrets:
      GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      NPM_TOKEN: ${{ secrets.NPM_TOKEN }}
    with:
      instance_id: dcs

  release:
    needs: test
    uses: kurocado-studio/devops/.github/workflows/workflow.release.yml@main
    secrets:
      GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      PAT_FORCE_PUSH: ${{ secrets.PAT_FORCE_PUSH }}
      NPM_TOKEN: ${{ secrets.NPM_TOKEN }}
    with:
      branch_name: ${{ github.ref_name }}
      monorepo: true

  deploy-systemhaus-service:
    uses: kurocado-studio/devops/.github/workflows/workflow.deploy.yml@main
    needs: test
    with:
      branch_name: ${{ github.ref_name }}
      prod_env: 'apps/systemhaus-service/fly.toml'
      dev_env: 'apps/systemhaus-service/fly.staging.toml'
    secrets:
      NPM_TOKEN: ${{ secrets.NPM_TOKEN }}
      GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      FLY_API_TOKEN: ${{ secrets.FLY_API_TOKEN }}
